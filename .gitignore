# Extension signing keys. Anyone holding one can sign a package the browser
# accepts as a legitimate update to your extension. Never publish these.
tools/keys/
*.pem

# Superseded duplicate trees and one-off release artifacts, kept on disk for
# reference only. Everything they held now lives in extensions/ or Release/.
_archive/

node_modules/
dist/
*.log
Thumbs.db
desktop.ini
.DS_Store
