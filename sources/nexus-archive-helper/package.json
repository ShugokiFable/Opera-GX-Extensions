{
  "name": "nexus-archive-helper",
  "version": "1.1.1",
  "description": "Chromium extension and Tampermonkey userscript for resolving Nexus Mods archived and hard-to-find file records through the official API.",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.js",
    "verify:build": "node scripts/verify-build.js",
    "verify": "npm test && npm run build && npm run verify:build",
    "version:set": "node scripts/set-version.js",
    "release": "node scripts/release.js",
    "release:unsigned": "node scripts/release.js --no-crx",
    "sync:opera": "node scripts/sync-opera-repo.js"
  }
}
